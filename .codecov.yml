ignore:
  - "cmd/**"
  - "api/**"
coverage:
  status:
    project:
      default:
        # 'auto' compares coverage against the base commit
        target: auto 
        # Allows coverage to drop by up to 1% without failing the check
        threshold: 1%